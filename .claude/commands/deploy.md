Run the deploy.bat script to build and deploy Halloumi.Shuffler to %LOCALAPPDATA%\Halloumi\Shuffler.

Run the following command and show the output:

```bash
cmd /c "cd /d e:\\Github\\Halloumi.Shuffler && deploy.bat"
```
