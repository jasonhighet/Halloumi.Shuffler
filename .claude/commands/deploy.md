PRODUCTION DEPLOYMENT ONLY. Builds and copies Halloumi.Shuffler to %LOCALAPPDATA%\Halloumi\Shuffler.

WARNING: Do NOT use this as a build command. Use /build to compile and check for errors.
Only run this when the user has explicitly asked to deploy a tested, ready build.

Run the following command and show the output:

```bash
cmd /c "cd /d e:\\Github\\Halloumi.Shuffler && deploy.bat"
```
